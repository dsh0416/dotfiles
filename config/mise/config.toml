[settings]
# Recognize project-local version files while replacing nvm, pyenv, and RVM.
idiomatic_version_file_enable_tools = ["node", "python", "ruby"]

[tools]
"cargo:cargo-clean-global" = "latest"
